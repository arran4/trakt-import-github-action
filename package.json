{
    "name": "trakt-import-github-action",
    "version": "1.0.0",
    "description": "Import streaming site watch logs to trakt by updating it",
    "main": "dist/index.js",
    "dependencies": {
        "@actions/core": "^1.9.1",
        "@actions/github": "^5.0.3",
        "csv-parse": "^5.2.1",
        "node-fetch": "^3.2.10",
        "trakt.tv": "^8.1.1"
    },
    "devDependencies": {
        "esbuild": "^0.14.48"
    },
    "scripts": {
        "build": "esbuild index.ts --bundle --outfile=index.js --platform=node"
    }
}
